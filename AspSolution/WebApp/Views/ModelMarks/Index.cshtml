@model IEnumerable<Domain.ModelMark>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.mark)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.model)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.mark)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.model)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ModelMarkID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ModelMarkID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ModelMarkID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
