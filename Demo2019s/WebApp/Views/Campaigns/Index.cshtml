@model IEnumerable<Domain.Campaign>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Service)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NameOfCampaign)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Service.NameOfService)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NameOfCampaign)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CampaignId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CampaignId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CampaignId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
