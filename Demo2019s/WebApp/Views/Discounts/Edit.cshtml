@model Domain.Discount

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Discount</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="DiscountId" />
            <div class="form-group">
                <label asp-for="CheckId" class="control-label"></label>
                <select asp-for="CheckId" class="form-control" asp-items="ViewBag.CheckId"></select>
                <span asp-validation-for="CheckId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="WashId" class="control-label"></label>
                <select asp-for="WashId" class="form-control" asp-items="ViewBag.WashId"></select>
                <span asp-validation-for="WashId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
