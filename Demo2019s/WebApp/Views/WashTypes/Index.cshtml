@model IEnumerable<Domain.WashType>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.WashId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NameOfWash)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.WashId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NameOfWash)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.WashTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.WashTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.WashTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
